[core]
  excludesfile = ~/.gitignore
  editor = vim
  autocrlf = input
#  safecrlf = warn
  hideDotFiles = dotGitOnly
[user]
  name = Jason Karns
  email = jason@karns.name
[github]
  user = jasonkarns
[credential]
  helper = osxkeychain
[alias]
  st = status
  ci = commit
  br = branch
  co = checkout
  cam = commit --amend -C HEAD
  hist = log --graph --date=short --pretty=format:\"%C(yellow)%h%C(reset) %C(green)%ad%C(reset) %C(red)|%C(reset) %s%d %C(bold blue)%an%C(reset)\"
  glog = log --graph --date=relative

  diffstat = diff --stat
  diffword = diff --word-diff
  chry = cherry-pick
[format]
  pretty=format:%C(yellow)%h%C(reset) -%C(red)%d%C(reset) %s %C(green)(%ar) %C(bold blue)%an%C(reset)
[branch]
  autosetupmerge = true
  autosetuprebase = always
#[branch "master"]
#  remote = origin
#  merge = refs/heads/master
[advice]
  statusHints = false
[color]
  branch = auto
  diff = auto
  grep = auto
  interactive = auto
  showbranch = auto
  status = auto
  ui = auto
[color "branch"]
  current = reverse green
  local = normal
  remote = red
  #plain = auto
[color "diff"]
  old = red
  new = green
  plain = normal
  whitespace = reverse normal
  meta = reverse cyan
  frag = ul cyan
  func = magenta
#  commit =
[color "status"]
  header = normal
  added = blue
  changed = green
  untracked = cyan
  branch = reverse normal
  nobranch = red
[diff]
#  tool = bc3
  wordRegex=[A-z0-9_]+|[^[:space:]]
[difftool]
  prompt = false
[merge]
#  tool = bc3
  defaultToUpstream = true
[pull]
  rebase = true
[push]
  default = tracking
[rerere]
  enabled = 1

[difftool "bc3"]
  path = c:/program files (x86)/beyond compare 3/bcomp.exe
[mergetool "bc3"]
  path = c:/program files (x86)/beyond compare 3/bcomp.exe
  trustExitCode = true
[difftool "winmerge"]
  cmd = 'c:/program files/winmerge/winmergeu.exe' \"$LOCAL\" \"$REMOTE\"
[mergetool "winmerge"]
  cmd = 'c:/program files/winmerge/winmergeu.exe' \"$LOCAL\" \"$REMOTE\" \"$MERGED\"
  trustExitCode = false
