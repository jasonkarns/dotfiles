# vi: ft=dosini

[default]
region = us-east-1

; [sso-session sesh-name]
; sso_region = us-east-1
; sso_start_url = https://start.us-gov-home.awsapps.com/directory/d-ACCTNUM
; [profile mhq-uat]
; sso_session = sesh-name
